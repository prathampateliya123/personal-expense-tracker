# Dev: Vite proxies /api → http://localhost:5000 (recommended, avoids CORS)
VITE_API_BASE_URL=/api

# Or call API directly (requires server CORS for your frontend origin):
# VITE_API_BASE_URL=http://localhost:5000/api
