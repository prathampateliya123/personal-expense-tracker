PORT=5000

# MongoDB Atlas (replace YOUR_DB_PASSWORD with your Atlas database user password)
MONGO_URI=mongodb+srv://prathampateliya06_db_user:YOUR_DB_PASSWORD@cluster0.iiytvhd.mongodb.net/expense-tracker?retryWrites=true&w=majority&appName=Cluster0

JWT_SECRET=your_jwt_secret_key_here_change_this
JWT_EXPIRE=7d
# Comma-separated if multiple frontend URLs (localhost + 127.0.0.1)
CLIENT_URL=http://localhost:5173,http://127.0.0.1:5173

# Optional SMTP for password reset emails (if not set, reset link logs to server console in dev)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM="ExpenseTracker <your_email@gmail.com>"
